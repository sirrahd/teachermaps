<header id="header" class="navbar navbar-static-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to t('global.app_name'), root_path, class: "brand" %>
      <nav>
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <div class="nav-collapse collapse">
          <ul class="nav pull-right">
            <% if signed_in? %>
              <%= render 'shared/feedback_form' %>
              <li id="fat-menu" class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  <%= current_user.name %> <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to 'Maps', current_user %></li>
                  <li><%= link_to 'Resources', 'Javascript:void();', id: 'show_resources' %></li>
                  <li><%= link_to t('signin.settings'), settings_path %></li>
                  <li class="divider"></li>
                  <li>
                    <%= link_to t('signin.signout'), signout_path, method: "delete" %>
                  </li>
                </ul>
              </li>
            <% else %>
              <li><%= link_to t('signup.link'), signup_path %></li>
              <li><%= link_to t('signin.link'), signin_path %></li>
            <% end %>
          </ul>
        </div>
      </nav>
    </div>
  </div>
</header>
<script type="text/javascript">
$(document).ready(function() {

  $(document).on('click', '#show_resources', function(){
    window.location = '<%= user_path(current_user, anchor:"resources") %>';
    window.location.reload(true);
    return true;
  });

});
</script>